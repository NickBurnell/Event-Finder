"use strict";

const favoritesList = {
    template: `
    
    `,
    controller: ["EventRequest", function(EventRequest) {
        const vm = this;
    }]
}

angular
    .module("EventApp")
    .component("favoritesList", favoritesList);